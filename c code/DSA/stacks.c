#include<stdio.h>
struct elements{
	int top;
	int size;
}init(top=-1; size=0);
int is_empty(){
	return(top==-1);
	}
int is_full(){
	return(top==size-1);
	}
int top(){
	return elements[]
void main(){
	
	